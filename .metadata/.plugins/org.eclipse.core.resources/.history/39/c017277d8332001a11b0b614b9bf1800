<!DOCTYPE html>
<html>
<head>
	<meta charset="EUC-KR">
	<title>Insert title here</title>
	<script type="text/javascript">
		//모두 선택 버튼을 누르면 food라는 이름으로 정의된 체크박스를 제어
		function allcheck(){
			//food 라는 이름으로 정의된 체크박스가 여러 개 = 베열로 관리 = 그래야 전체로 제어할 수 있다.
			mycheck = document.cbfmt.food;
			alert(mycheck);
			alert(mycheck.length);
			
			//allchk 값을 읽어서 푸드 체크박스 값에 동일하게 설정
			//allchk 선택 => food 체크박스 모두 선택. 해제도 마찬가지
			allChkVal = cbfmt.allchck.checked;
			//alert(allChkVal); 선택은 true, 해제는 false
			for(i=0;i<mycheck.length;i++){
				mycheck[i].checked = allChkVal;
			}
			
			
			//mydiv = document.getElementById("result"); 겟터
			//innerHTML은 문자열 컨테츠를 추가 - 기존의 컨텐츠를 덮는다.
			//p, div, span에서만 사용할 수 있다.
			document.getElementById("result").innerHTML = 
				"<h1>결과</h1>";
		}
		function ckFunc(){
			/* 
			1. food 체크박스를 변수에 저장하는 작업 
			2. for문으로 food체크박스의 값을 하나하나 클릭해보기
			3. 선택된 체크박스만 값을 출력하도록 조건제어
			4. 선택된 체크박스의 값을 누적시켜 변수에 저장하기
			5. 체크된 체크박스의 값을 div에 출력하기
			*/
		}
		function printText(){
			myval = cbfmt.test.value; //getter
			//alert(myval); 테스트 확인 후 주석처리
			document.getElementById("result").innerHTML = 
				"<h1>"+myval+"</h1>";
		}
	
	</script>
</head>

<body>
	<!-- <center> 서식은 CSS에서 해야 한다. -->
		<form name="cbfmt">
			<label>아이스크림</label> 
				<input type="checkbox" name="food" value="아이스크림" id="ice"> 
			<label>커피</label> <input type="checkbox"name="food" value="커피" id="coffee">
			<label>음료수</label> <input type="checkbox" name="food" value="음료수" id="beverage"> 
			<label>모두체크/해제</label>
			<input type="checkbox" name="allchck" 
			id="allchk" onclick="allcheck()"><br/>

			<input type="button" value="전송" onclick="ckFunc()">
			<input type="text" name="test" onkeyup="printText()">
			
		</form>
		<div id="result"></div> <!-- DOM으로 접근해야함 -->
	<!-- </center> -->
</body>
</html>