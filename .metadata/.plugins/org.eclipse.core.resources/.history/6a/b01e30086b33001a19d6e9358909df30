<!DOCTYPE html>
	<html>
	<head>
	<script type="text/javascript">
	
	function insertP(){
		//alert("test");
		pNode = document.createElement("p"); //p 만들기
			pNode.setAttribute("style","background-color: yellow;"); //색지정
		TextNode = document.createTextNode("newP"); //newP 만들기
		pNode.appendChild(TextNode); //newP를 p밑으로 넣기
		parentNode = document.getElementById("root"); //부모 노드 지정
		parentNode.appendChild(pNode); //p를 부모 노드안으로 넣기

		}
	
	function deleteP(){
		parentNode = document.getElementById("root");
			/* if(parentNode.lastChild.nodeName=="#text"){
				parentNode.removeChild(parent.lastChild);
			}
		parentNode.removeChild(parentNode.lastChild); 공백포함*/
		parentNode.removeChild(parentNode.lastElementChild); //공백 미포함
		
	}
	function deleteDivAndP(){
		//root의 모든 하위 엘리먼트 제거
		parentNode = document.getElementById("root");
		childnodelist = parentNode.childNodes;
		for(i=0;i<childnodelist.length;i++){
			parentNode.removeChild(parentNode.lastChild); //돌고 있는데 하나씩 제거하고 있으므로
													//for문 길이가 줄어서 전부 다 처리를 못 한다
		}
	}
	function innerHTML_test(){
		
	}
	</script>
<meta http-equiv="Content-Type" content="text/html; charset=EUC-KR">
<title>Insert title here</title>
</head>
<body>
	<div id="layer"></div>

	<hr>

	<div id="root">
		<p style="background: gray;">line01</p>
		<p style="background: skyblue;">line02</p>
		<p style="background: green;">line03</p>
		<p style="background: red;">line04</p>
	</div>

	<hr>
	<button onclick="insertP()">P태그추가하기</button>
	<button onclick="deleteP()">P태그삭제하기</button>
	<button onclick="deleteDivAndP()">div안의 모든 p태그 삭제하기</button>
	<button onclick="innerHTML_test()">innerHTML</button>


</body>
</html>